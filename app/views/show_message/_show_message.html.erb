<% data.each do |m| %>
  <div class="alert-box <%= m[:class] %> <%= "autohide" if options[:autohide] %>" style= "<%= (m != data.last) ? "margin-bottom:5px;" : "" %>">
    <div class="message">
      <%= raw m[:message] %>
    </div>
  </div>
<% end %>